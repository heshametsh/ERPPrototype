RevoGrid Community — ERP Gate 4B

Single regression fix from Gate 4:
- ERP Undo/Redo cell operations now use the same proven source refresh strategy that passed Gate 2.
- Removed the Gate 4 setDataAt refresh experiment from the history path.
- New file names are used to avoid browser cache confusion.

No Work Orders production files are modified.
