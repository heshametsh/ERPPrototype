RevoGrid Community Guided Gate 2.5

Purpose:
- Validate an ERP-owned Community-only Excel-like header behavior.
- Click column title => select whole column.
- Dedicated ↕ area => native sort.
- RevoGrid Community filter button remains independent.
- Filter success is detected from beforefiltertrimmed hidden-row count, not the unreliable previous visible-count harness.

No Work Orders runtime file is changed. This is grid-shootout only.
