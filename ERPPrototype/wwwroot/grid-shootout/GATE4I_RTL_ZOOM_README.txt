RevoGrid Community — Gate 4I — RTL + Zoom Focused

Already proven and NOT repeated:
- 100,000 rows core Gate 3
- ERP Edit/Dirty/Save/Undo/Redo
- 5,000-value paste + ERP-owned history
- Readonly
- Delete/Undo/Redo 1,000 rows
- Custom Columns + Undo/Redo
- Native Split/Full selection stability 4/4

This gate tests only:
1) Manual RTL switch -> user selects Basket -> user scrolls hard.
2) Manual browser Zoom: 90% -> 80% -> 67% -> 100%.

The page performs no scrolling and no zooming for the user.
It only observes and judges selection stability.

No Work Orders production files are modified.
