RevoGrid Community — ERP Gate 4E Manual

Single fix from Gate 4D:
- Fixed invalid JavaScript token on the afteredit condition:
  !===  ->  !==

No test behavior was changed.
The page remains fully manual: the user performs the actions; the page only observes/verifies.

No Work Orders production files are modified.
