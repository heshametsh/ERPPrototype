RevoGrid Community Guided Gate 2.6

Purpose:
- Fix real mouse header selection in Community mode.
- Intercept column-title header click before RevoGrid default focus/sort behavior.
- Keep Sort on the separate arrow and Filter on its own icon.
- Observe filtering through both filter-specific trimming and generic trimming events.
- Work Orders runtime is untouched; this is grid-shootout only.
