ERP Prototype Work Orders — v38 Responsive Playground

Base: exact v34 copy.

Experiment only:
- Removes page min-width.
- Table owns horizontal scrolling and starts from the first column.
- Basket width is free-dragged and stored as a ratio of the workspace, not fixed pixels.
- Basket content adapts to the actual panel width: compact / summary / full.
- No JavaScript full/split/zoom layout modes.
- v34 fixed reference remains untouched in ../v34-fixed.
