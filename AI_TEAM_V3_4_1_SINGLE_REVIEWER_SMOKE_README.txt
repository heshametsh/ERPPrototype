ERP AI Team V3.4.1 - Single Reviewer Smoke

Purpose
- Prove one real reviewer end-to-end before spending allowance on three concurrent reviewers plus Lead.
- Uses the already-qualified local router; no AI router call.
- Starts exactly one requested reviewer selected by that local route.
- Runs reviewer structured output + Finding Gate + evidence validation + cleanliness.
- Deliberately does NOT start sibling reviewers or Lead.
- Captures exact Codex attempts, completed calls, tokens, and timing.

Recommended qualification command
  erp-ai-team smoke-reviewer AIT-02 revo

Expected model-backed calls
  1 reviewer call only.

No ERP runtime files are changed by this patch.
